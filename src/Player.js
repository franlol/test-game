'use strict'

class Player extends Character {

    constructor(canvas, game) {
        super(canvas, game);
        this.speed = 6;
        // this.canvas = canvas;                            //SUPER
        // this.ctx = this.canvas.getContext("2d");         //SUPER
        // this.direction = 0;                              //SUPER
        // this.sizeX = 40;                                 //SUPER
        // this.sizeY = 60;                                 //SUPER
        // this.lives = 3;                                  //SUPER
        // this.x = (canvas.width / 2) - (this.sizeX / 2);  //SUPER
        // this.y = canvas.height - 80;                     //SUPER
    }

    
}